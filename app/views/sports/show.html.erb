
<div class='container'>
  <h1><%= @sport.name %></h1>

  routines for this sport:
  <ul>
    <% @routines.each do |r| %>
    <li><%= r.name %> <%= r.description %> <%= link_to 'go', routine_path(r) %></li>
    <% end %>
  </ul>
</div>
