<div class='container'>
  <div class='header'>
    <h1>Welcome to Gasho</h1>
    <p>*search bar goes here*</p>
  </div>
  <h3>Special Sport routines</h3>
  <div class="owl-carousel">
    <% @sports.each do |s| %>
      <%= link_to sport_path(s) do %>
        <div class='sport-card card p-5'>
          <div class='text-center'><%= s.name %></div>
        </div>
      <% end %>
    <% end %>
  </div>
  <h3>My routines <%= link_to '+', new_routine_path, class:'btn btn-primary rounded-circle' %></h3>
  <% if @myroutines %>
    <div class="owl-carousel">
      <% @myroutines.each do |s| %>
        <%= link_to routine_path(s) do %>
          <div class='sport-card card p-5'>
            <div class='text-center'><%= s.name %></div>
          </div>
        <% end %>
      <% end %>
      <% end %>
    </div>
    <%= link_to 'yoga centers near me','#', class:'btn btn-primary' %>
</div>



<%= content_for(:after_js) do %>
  <script>
    $(document).ready(function(){
    $('.owl-carousel').owlCarousel({
        stagePadding: 50,
        loop: true,
        margin:10,
        responsive:{
          0:{
            items:1
          },
          600:{
          items:3
          }
        }
      })
    });
  </script>
<% end %>
