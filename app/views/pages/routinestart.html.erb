<div id="carouselExampleSlidesOnly" class="vertical carousel slide" data-bs-ride="carousel" data-interval="1000">
  <div class="carousel-inner">
    <div class="carousel-item active">
      <img src=<%= stretch.photo %> class="d-block w-100" alt="...">
    </div>
  <% @stretches.each do |stretch| %>
    <div class="carousel-item">
      <img src=<%= stretch.photo %> class="d-block w-100" alt="...">
    </div>
  <% end %>
  </div>
</div>
